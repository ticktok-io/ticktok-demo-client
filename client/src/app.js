import React, { Component } from 'react';
import SearchBar from "./modules/clock/clock_creator";

export default class App extends Component {
  render() {
    return (
      <div>
        <SearchBar/>
      </div>
    );
  }
}
